# SAINTAKER Backend

This is the backend service for the SAINTAKER application built with FastAPI.

## Getting Started

To run the backend service:

```bash
cd ~/repos/SAINTAKER/backend
poetry install
poetry run uvicorn app.main:app --reload
```

The API will be available at http://localhost:8000

